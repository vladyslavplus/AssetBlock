# Monorepo root — build/tooling leftovers (e.g. `dotnet ef` MSBuild targets)
[Oo]bj/
[Bb]in/

# Root tooling (husky / lint-staged)
node_modules/

# Generated dependency governance artifacts (SBOMs are CI/local outputs)
artifacts/

# Local AI planning artifacts
**/.cursor/plans/
